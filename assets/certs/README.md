# Pinned Certificates

Place SHA-256 public-key pins or `.pem` leaf/intermediate certificates here.
They are referenced by `SslPinningService` and the `CertificatePinningInterceptor`.
Never commit real production certificates to a public repository.
